#include <stdio.h>
/**
  *main -Entry point
  *Description - program to print single base ten numbers
  *Return: Always 0
  */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
